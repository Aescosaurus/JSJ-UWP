class Game
{
	constructor( wnd = new _Window )
	{
		// Variables go here.

		// 
		// Initialize is called at the start of the game.
		this.Initialize = () =>
		{

		};
    
		// Update is called once per frame.
		this.UpdateGame = () =>
		{

		};

		// Draw graphics to screen.
		this.ComposeFrame = () =>
		{

		};
	}
}